# 头像后续待办

## 已选头像待替换为彩色
- 贝特霍尔德·胡佛：当前使用 1 号，本轮先保留黑白版，后续替换为彩色。
- 贾碧·布朗：当前使用 1 号，本轮先保留黑白版，后续替换为彩色。
- 格里沙·耶格尔：当前暂按 1 号接入；你这次没有写编号，只标了“黑白”，先按 1 号处理，后续可再改。
- 萨莎·布劳斯：当前使用 2 号，本轮先保留黑白版，后续替换为彩色。
- 吉克·耶格尔：当前使用 2 号，本轮先保留黑白版，后续替换为彩色。

## 新角色待补头像
- 多托·匹西斯：角色已接入，结果页先显示“头像待确认”；后续补抓取候选并生成 `public/portraits-selected/dot_pyxis.png`。
- 达里斯·萨克雷：角色已接入，结果页先显示“头像待确认”；后续补抓取候选并生成 `public/portraits-selected/dhalis_zachary.png`。

## 特殊处理
- 阿明·阿诺德：当前使用 3 号，并在结果页做了头部到肩膀的聚焦裁剪。
